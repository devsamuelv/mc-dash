# Nextjs-Typescript

- **Librarys Used**

  - PulseJS
  - Tailwindcss

- **Run** `yarn install` to install all the packages

### Function Template

```ts
type Props = {
	username: string;
	message: string;
};

export const myFunction: React.FunctionComponent<Props> = () => {};
```
